import React, { FC } from "react";
import { Card } from "react-bootstrap";

interface GeneralAvailabilityFormProps {
    groupId: string;
}

const GeneralAvailabilityForm: FC<GeneralAvailabilityFormProps> = (props) => {
    return (
        <>
            <style>
                {`
                
                `}
            </style>
            
        </>
    );
}

export default GeneralAvailabilityForm;